# Minimal Nginx: serve only static JSON (no TLS here — add TLS at edge/CDN).
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location = /relay-pin-manifest.json {
        default_type application/json;
        add_header Cache-Control "public, max-age=300";
    }
}
